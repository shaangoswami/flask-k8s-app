apiVersion: apps/v1
kind: Deployment
metadata:
  name: webserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webserver
  template:
    metadata:
      labels:
        app: webserver
    spec:
      containers:
      - name: webserver
        image: shaangoswami/flask-app:latest 
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          
